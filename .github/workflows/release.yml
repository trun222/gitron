name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
            rust-targets: 'aarch64-apple-darwin'
          - platform: macos-latest
            args: '--target x86_64-apple-darwin'
            rust-targets: 'x86_64-apple-darwin'
          - platform: ubuntu-22.04
            args: ''
            rust-targets: ''
          - platform: windows-latest
            args: ''
            rust-targets: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: '. -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: 'Gitron v__VERSION__'
          releaseBody: |
            See the assets below to download and install Gitron v__VERSION__.

            ## Installers
            - **Windows**: `.msi` or `.exe` (NSIS)
            - **macOS**: `.dmg`
            - **Linux**: `.deb` or `.AppImage`

            ## macOS: "Gitron is damaged" warning
            macOS may show a "damaged and can't be opened" error for unsigned builds. This is a Gatekeeper quarantine issue, not actual damage. To fix it, run the following after installing:
            ```
            xattr -c /Applications/Gitron.app
            find /Applications/Gitron.app -exec xattr -c {} \;
            ```

            ## Windows: SmartScreen warning
            Windows may flag the installer as unrecognized. Click **"More info"** → **"Run anyway"** to proceed. This warning will go away once the app is code-signed in a future release.
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

  release-server:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact: gitron-server-linux-x86_64
          - platform: macos-latest
            target: aarch64-apple-darwin
            artifact: gitron-server-macos-arm64
          - platform: macos-latest
            target: x86_64-apple-darwin
            artifact: gitron-server-macos-x86_64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: gitron-server-windows-x86_64.exe

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: '. -> target'

      - name: Build server
        run: cargo build -p gitron-server --release --target ${{ matrix.target }}

      - name: Rename artifact (Unix)
        if: matrix.platform != 'windows-latest'
        run: cp target/${{ matrix.target }}/release/gitron-server ${{ matrix.artifact }}

      - name: Rename artifact (Windows)
        if: matrix.platform == 'windows-latest'
        run: copy target\${{ matrix.target }}\release\gitron-server.exe ${{ matrix.artifact }}

      - name: Upload server binary
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
